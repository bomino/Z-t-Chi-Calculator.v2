name = "ztchi-backend"
main = "worker.js"
compatibility_date = "2024-11-01"

# Allowed origins for CORS. Update to match your GitHub Pages URL.
# Multiple origins can be listed, one per line.
[vars]
ALLOWED_ORIGINS = "https://bomino.github.io,http://localhost:8804,http://127.0.0.1:8804"
RATE_LIMIT_PER_HOUR = "60"
AI_MODEL = "claude-haiku-4-5-20251001"

# Secrets are set via: wrangler secret put <name>
# Required secrets:
#   SIGN_SECRET         — HMAC-SHA256 secret for signing instructor problems
#   ANTHROPIC_API_KEY   — Anthropic Claude API key
# Optional secret:
#   INSTRUCTOR_TOKENS   — comma- or newline-separated list of per-instructor
#                         tokens. When set, /sign requires a Bearer token
#                         matching an entry. When unset, any origin in the
#                         allowlist can sign.
